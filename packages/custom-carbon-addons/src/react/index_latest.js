// for each specific custom addon component, create a separate directory in the
// 'addons' directory which will contain the React JavaScript for that addon component.

// If there are custom components that have common functionality to the specific ones
// ones in 'addons', you could create a common directory that contains these common
// addon components and the others in 'addons' can refer to them.
